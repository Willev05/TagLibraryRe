<UserControl x:Class="TagLibraryRe.View.UserControls.FilesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TagLibraryRe.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Menu Background="Transparent" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5">
            <MenuItem Header="File" FontSize="16">
                <MenuItem Header="New" Command="{Binding NewFile}"/>
                <MenuItem Header="Load" Command="{Binding LoadFile}"/>
            </MenuItem>
            <MenuItem Header="Save" FontSize="16" Command="{Binding Save}"/>
        </Menu>
    </Grid>
</UserControl>
