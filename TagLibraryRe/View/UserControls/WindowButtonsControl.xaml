<UserControl x:Class="TagLibraryRe.View.UserControls.WindowButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TagLibraryRe.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button x:Name="btnClose" Content="X" Grid.Column="2" Command="{Binding CloseCommand}"/>
        <Button x:Name="btnMaximize" Content="Max" Grid.Column="1" Command="{Binding MaximizeCommand}"/>
        <Button x:Name="btnMinimize" Content="_" Command="{Binding MinimizeCommand}"/>
        
    </Grid>
</UserControl>
